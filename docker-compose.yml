version: "3.9"

networks:
  nologo_proxynet:
    external: true
  nologo_internal:
    external: false

volumes:
  nologo_ruby_bundle:
    external: true
  db_data:
  letsencrypt:
  redis_data:

services:
  traefik:
    image: traefik:v2.3
    ports:
      - "3000:3000"
      - "8088:8080" # traefik dashboard
    command:
      - --api.insecure=true
      - --api.dashboard=true
      - --api.debug=true
      - --providers.docker=true
      - --providers.docker.exposedbydefault=false
      - --providers.docker.network=nologo_proxynet
      - --entrypoints.web.address=:3000
      - --certificatesresolvers.mytlschallenge.acme.httpChallenge.entrypoint=web
      - --certificatesresolvers.mytlschallenge.acme.email=ryantgray@gmail.com
      - --certificatesresolvers.mytlschallenge.acme.storage=/letsencrypt/acme.json
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - letsencrypt:/letsencrypt
    networks:
      - nologo_proxynet
  nologo_app:
    image: monitorbot.the4909.net:5050/goat/dockerfiles/rails-7.0.0:latest
    environment:
      - REDIS_URL=redis://redis:6379/0
      - REDIS_URL_SIDEKIQ=redis://redis:6379/0
    volumes:
      - nologo_ruby_bundle:/usr/local/bundle
      - ./:/app
    working_dir: /app
    networks:
      - nologo_proxynet
      - nologo_internal
    labels:
      - traefik.enable=true
      - traefik.http.routers.nologo_app-router.entrypoints=web
      # - traefik.http.routers.nologo_app-router.rule=Host(`goatbot.the4909.net`) && PathPrefix(`/documentation`)
      - traefik.http.routers.nologo_app-router.rule=Host(`goatbot.the4909.net`)
      - traefik.http.routers.nologo_app-router.service=nologo_app-service
      - traefik.http.services.nologo_app-service.loadbalancer.server.port=3000
  # sidekiq:
  #   image: monitorbot.the4909.net:5050/goat/dockerfiles/rails-7.0.0:latest
  #   environment:
  #     - REDIS_URL=redis://redis:6379/0
  #     - REDIS_URL_SIDEKIQ=redis://redis:6379/0
  #   volumes:
  #     - nologo_ruby_bundle:/usr/local/bundle
  #     - ./:/app
  #   working_dir: /app
  #   networks:
  #     - nologo_internal
  #   labels:
  #     - traefik.enable=false
  #   command: bundle exec sidekiq
  # db:
  #   image: postgres:13-alpine
  #   volumes:
  #     - db_data:/var/lib/postgresql/data
  #   environment:
  #     POSTGRES_PASSWORD: password
  #   networks:
  #     - nologo_internal
  redis:
    image: redis:alpine3.15
    volumes:
      - redis_data:/data
    labels:
      - traefik.enable=false
    networks:
      - nologo_internal